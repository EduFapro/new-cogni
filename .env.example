# Example environment file for Novo Cogni
# Copy this to .env and fill in real values before running the app.

# ==========================================
# APP MODE CONFIGURATION
# ==========================================
# Choose one of the following modes:
# 1. offline : Standalone mode. Uses local SQLite only. No backend connection.
# 2. local   : Development mode. Connects to a local backend (no JWT auth).
# 3. remote  : Production mode. Connects to a remote backend (JWT auth enforced).
APP_MODE=local

# ==========================================
# BACKEND CONFIGURATION (Required for local/remote)
# ==========================================
# For Android Emulator accessing localhost, use: http://10.0.2.2:8080
# For Windows/iOS Simulator accessing localhost, use: http://localhost:8080
# For Remote, use the actual URL: https://api.example.com
# For Remote, use the actual URL: https://api.example.com
BACKEND_URL=http://localhost:8080

# Set to true to enable backend synchronization
ENABLE_SYNC=true

# ==========================================
# SECURITY CONFIGURATION (Required)
# ==========================================
# Must be exactly 32 characters
ENCRYPTION_KEY=CHANGE_THIS_TO_A_32_CHAR_SECRET_KEY

# Must be exactly 16 characters
ENCRYPTION_IV=CHANGE_THIS_TO_A_16_CHAR_IV
